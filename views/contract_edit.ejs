<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/style.css" charset="utf-8">
    <title>KillBase - Edit Contract</title>
</head>
<body>
    <form method="POST" action="/contracts_all/update">
    <% include ./partials/nav %>
        
            <div class='div_assassin'>
                <table>
                    <tr>
                        <td colspan="2"><h3>Edit Contract</h3></td>
                    </tr>
                    <tr>
                        <td><img class="thumb" src="<%= contracts[0].target_photo %>"></td>
                        <td>Contract ID: <%= contracts[0].assassin_name %>
                        <br/>Target: <input type="text" name="target_name" value="<%= contracts[0].target_name %>">
                        <br/>Security Level: <input type="text" name="security" value="<%= contracts[0].security %>">
                        <br/>Location: <input type="text" name="location" value="<%= contracts[0].location %>">
                        <br/>Budget: $<input type="text" name="budget" value="<%= Math.round(contracts[0].budget) %>">K
                        <br/>Client: <%= contracts[0].client_name %>
                        <br/>Completed: <input type="text" name="completed" value="<%= contracts[0].completed %>">
                        <br/><select name="completed_by">
                                <option value="" selected disabled hidden>Job completed by...</option>
                            <% for(let i=0; i<assassins.length; i++) { %>
                                <option value="<%= assassins[i].assassin_id %>"><%= assassins[i].assassin_name %> (<%= assassins[i].code_name %>)</option>
                            <% } %>
                        </select>
                        <input type="hidden" name="contract_id" value="<%= contracts[0].contract_id %>">
                        <input type="hidden" name="contract_target_id" value="<%= contracts[0].contract_target_id %>">
                        <input type="hidden" name="contract_client_id" value="<%= contracts[0].contract_client_id %>">
                        </td>
                    </tr>
                    <tr>
                        <td colspan="2">
                            <button type="submit">SUBMIT</button>
                        </td>
                    </tr>
                </table>
            </div>

    </form>
</body>
</html>